apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
  namespace: prod
  labels:
    app: webapp
    type: webapp
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      name: webapp
      labels:
        app: webapp
        type: webapp
        env: prod
    spec:
      containers:
        - name: webapp
          image: tutum/hello-world
---
apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: prod
  labels:
    app: webapp
spec:
  type: NodePort
  ports:
   - port: 80
  selector:
   app: webapp